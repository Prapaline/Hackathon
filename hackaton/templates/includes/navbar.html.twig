 <nav class="vertical-navbar">
            <a class="navbar-brand fw-bold" href="{{ path('app_user_index') }}">
                <i class="fas fa-rocket"></i>
            </a>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' }}" href="{{ path('app_home') }}">
                        <i class="fas fa-home"></i>
                        <span>Accueil</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_chantier_index' ? 'active' }}" href="{{ path('app_chantier_index') }}">
                        <i class="fas fa-concierge-bell"></i>
                        <span>Projects</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_user_index' ? 'active' }}" href="{{ path('app_user_index') }}">
                        <i class="fas fa-envelope"></i>
                        <span>Employés</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_skill_index' ? 'active' }}" href="{{ path('app_skill_index') }}">
                        <i class="fas fa-envelope"></i>
                        <span>Skills</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_planning' ? 'active' }}" href="{{ path('app_planning') }}">
                        <i class="fas fa-envelope"></i>
                        <span>Planning</span>
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fas fa-user-circle"></i>
                        <span>Compte</span>
                    </a>
                    <ul class="dropdown-menu">
                        <!-- Ajoutez vos liens de menu déroulant ici -->
                    </ul>
                </li>
                <li>
                <a href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            </ul>
        </nav>

         <style>
            /* Styles personnalisés pour la navbar verticale */
            .vertical-navbar {
                width: 80px; /* Largeur de la navbar */
                height: 100vh; /* Hauteur totale de la vue */
                position: fixed; /* Fixe la navbar à gauche */
                top: 0;
                left: 0;
                background: linear-gradient(45deg, #3f51b5, #2196F3);
                box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
                display: flex;
                flex-direction: column;
                align-items: center;
                padding-top: 1rem;
            }

            .vertical-navbar .navbar-brand {
                margin-bottom: 2rem;
            }

            .vertical-navbar .nav-link {
                width: 100%;
                text-align: center;
                padding: 0.75rem 0 !important;
                margin: 0.25rem 0;
                color: white;
                transition: all 0.3s;
            }

            .vertical-navbar .nav-link:hover,
            .vertical-navbar .nav-link.active {
                background: rgba(255, 255, 255, 0.15);
            }

            .vertical-navbar .nav-link i {
                font-size: 1.25rem;
                margin-bottom: 0.25rem;
            }

            .vertical-navbar .nav-link span {
                display: block;
                font-size: 0.75rem;
            }

            .main-content {
                margin-left: 80px; /* Marge pour éviter que le contenu ne soit caché sous la navbar */
                padding: 1rem;
            }
        </style>