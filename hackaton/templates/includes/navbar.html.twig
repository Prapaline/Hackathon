<nav class="vertical-navbar">
    <div class="user-info">
        <span class="user-name">Employé : <strong>{{ last_name }}</strong> {{ first_name }}</span>
    </div>
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link {{ app.request.get('_route') == 'app_home' ? 'active' }}" href="{{ path('app_home') }}">
                <i class="fas fa-home"></i> Accueil
            </a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}

        <li class="nav-item">
            <a class="nav-link {{ app.request.get('_route') == 'app_chantier_index' ? 'active' }}" href="{{ path('app_chantier_index') }}">
                <i class="fas fa-hard-hat"></i> Chantiers
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link {{ app.request.get('_route') == 'app_user_index' ? 'active' }}" href="{{ path('app_user_index') }}">
                <i class="fas fa-users"></i> Employés
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link {{ app.request.get('_route') == 'app_skill_index' ? 'active' }}" href="{{ path('app_skill_index') }}">
                <i class="fas fa-cogs"></i> Skills
            </a>
        </li>
    {% endif %}

        <li class="nav-item">
            <a class="nav-link {{ app.request.get('_route') == 'app_planning' ? 'active' }}" href="{{ path('app_planning') }}">
                <i class="fas fa-calendar-alt"></i> Planning
            </a>
        </li>
    </ul>
    <div class="logout">
        <a href="{{ path('app_logout') }}" class="nav-link">
            <i class="fas fa-sign-out-alt"></i> Déconnexion
        </a>
    </div>
</nav>


<style>
.vertical-navbar {
    top: 0;
    left: 0;
    height: 100vh; /* Prendre toute la hauteur de l'écran */
    width: 250px; /* Largeur fixe de la sidebar */
    background: #434255;
    display: flex;
    flex-direction: column;
    justify-content: center; /* Centrer verticalement */
    padding: 2rem 0;
    align-items: center;
    padding: 2rem 0;
    box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
    color: white;
    padding-top: 1rem;
}
   

    .main-content {
        margin-left: 250px; /* Laisser de la place à la sidebar fixe */
        padding: 20px;
        width: calc(100% - 250px); /* Prendre tout l'espace restant */
        height: 100vh;
        overflow-y: auto; /* Permettre le défilement du contenu principal */
        z-index: 1; /* Assurer que le contenu est sous la navbar */
    } 
    .user-info {
        text-align: center;
        margin: 1rem 0px 2rem 0px;
    }

    .user-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        margin-bottom: 10px;
    }

    .user-name {
        font-size: 1rem;
        color: white;
    }

    /* Liens de navigation */
    .navbar-nav {
        width: 100%;
        padding: 0;
        margin: 0;
        list-style-type: none;
    }

    .navbar-nav .nav-item {
        width: 100%;
    }

    .vertical-navbar .nav-link {
        display: flex;
        align-items: center;
        width: 100%;
        padding: 1rem 2rem;
        text-align: left;
        color: white;
        font-size: 1rem;
        transition: background-color 0.3s ease;
    }

    .vertical-navbar .nav-link:hover,
    .vertical-navbar .nav-link.active {
        background-color: #ed9108; /* Fond plus clair au survol */
    }

    .vertical-navbar .nav-link i {
        font-size: 1.5rem;
        margin-right: 1rem;
    }

    .vertical-navbar .nav-link span {
        display: inline-block;
    }

    /* Section de déconnexion */
    .logout {
        margin-top: auto;
        width: 100%;
        padding: 1rem 1rem;
    }

    .logout .nav-link {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 12px 42px;
        color: white;
        font-size: 1.25rem;
        background-color:rgb(96, 95, 124);
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    .logout .nav-link:hover {
        background-color:rgb(82, 81, 107);
    }
</style>
